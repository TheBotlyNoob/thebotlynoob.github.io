---
import glob from 'fast-glob';

export async function getStaticPaths() {
  return glob('**/*.md').then((paths) =>
    paths.map((path) => ({ params: { path } }))
  );
}

const params = Astro.request.params;
---
