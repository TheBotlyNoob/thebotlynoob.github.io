---
export interface props {
  lang: string;
  inline?: boolean;
}

let { lang, inline, ...props } = Astro.props as props;
---

{inline ? (
  <code {...props} class={`language-${lang}`}>
    <slot />
  </code>
) : (
  <pre {...props}>
    <code class={`language-${lang}`}>
      <slot />
    </code>
  </pre>
)}
